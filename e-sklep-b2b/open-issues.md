# Open Issues (не блокируют переход к Этапу 1)

## 1. Содержательная сверка текста Task/чек-листов с текстом статей документации
Сделано: URL проверены на существование и актуальность версии (source-map.md).
Не сделано: построчная сверка, что шаги/формулировки в Task и чек-листах соответствуют
текущему интерфейсу и процессу, описанному в самой статье (не просто рабочая ссылка).

Приоритет для будущей сверки — M2, M3, M6 (наиболее техничные, выше риск расхождения с интерфейсом после обновлений Comarch).

## 2. `effort-estimates.yaml`: keyword-эвристика не покрывает 7 реальных Task
Найдено при реализации `agent/assemble_plan.py` (Этап 6) через `--selftest` на
всех Task M1–M9. Глагол либо не входит в `keywords` какого-либо типа, либо не
стоит в начале `Task.name` (например, T-9.3.1/T-8.1.1 называются через
"Определить...", хотя их `description` начинается с "Согласовать с
заказчиком..." — фактически это `negotiation`).

Список: T-2.1.2, T-5.5.1, T-6.2.5, T-7.1.2, T-7.2.2, T-8.1.1, T-9.3.1.

Не исправлено в `effort-estimates.yaml` (файл не менялся в этом проходе) —
вместо этого в `assemble_plan.py` заведён `MANUAL_TASK_TYPE_OVERRIDES` с
обоснованием по каждой Task. Требуется на будущее: либо расширить `keywords`
соответствующих типов, либо (что чище) добавить в `tasks/M*_tasks.yaml`
явное поле `task_type` — оба варианта меняют утверждённые Этапом 3/структуру
Task, поэтому не вносились по ходу (см. `v2_proposals` в `effort-estimates.yaml`).

## 3. `T-6.5.1.depends_on` и `T-6.4.1.used_by` ссылаются на слот `T-6.4.2`, который при непустых `integrations` не существует
Когда WBS-6.4 разворачивается в `T-6.4.2.1..N` (`docs/selection-rules.md`),
единственные в шаблоне Task, ссылающиеся на `T-6.4.2` (`T-6.5.1.depends_on` и
симметрично `T-6.4.1.used_by`), теряют эту ссылку, если её не переписать на
сгенерированные id — иначе граф зависимостей просто не замечает пропавший id,
а не сигналит об ошибке. Второй случай (`T-6.4.1.used_by`) найден Этапом 7
(`validate_plan.py`, проверка `integrity`), не сразу при реализации Этапа 6.

Не исправлено в `tasks/M6_tasks.yaml` (файл не менялся) — обработано в
`agent/assemble_plan.py` функцией `patch_stale_slot_dependencies` (для обоих
полей, `depends_on` и `used_by`) во время генерации плана, с регрессионной
проверкой в `--selftest` (и в `--selftest` у `validate_plan.py`). Если у
WBS-6.4 в будущем появится ещё один аналогичный "слот", эту функцию нужно
будет обобщить, а не копировать точечно.

---
Пункт 1 не блокирует работу над агентом (Этап 1), т.к. не влияет на структуру входа/выхода агента — переносится в backlog.
Пункты 2–3 не блокируют Этап 6 (обработаны на уровне `assemble_plan.py`), но со временем стоит закрыть в исходных артефактах Этапов 3 и 2.
