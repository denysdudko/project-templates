# Скопировать в agent/.env и заполнить -- используется agent/run_form.py
# (лаунчер локальной формы + create_project.py). Файл agent/.env в git не
# попадает (см. .gitignore).

JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
