# Правило распределения Milestone/WBS/Task по спринтам (Этап 5)

Документ описывает, как агент распределяет Task шаблона `e-sklep-b2b`
по спринтам конкретного проекта. Это правило для агента (логика
вычисления), а не изменение структуры шаблона: `schema/milestones_wbs.yaml`
и `tasks/M*_tasks.yaml` этим правилом не меняются и не должны знать о
номерах спринтов.

## Источник истины

Comarch не документирует Agile/спринт-планирование (это не описанный
вендором процесс) — правило целиком относится к
`source.type: Internal Project Methodology`, как и остальные
управленческие Task шаблона (M7/M8/M9, см. `docs/source-map.md`).
Официальная документация Comarch используется только там, где спринт
затрагивает продуктовые ограничения (например, длительность
milestone) — таких пересечений в этом правиле нет.

## Входные данные

| Источник | Что берётся |
|---|---|
| `agent/input-schema.json` | `start_date`, `target_launch_date` — границы проекта |
| `schema/milestones_wbs.yaml` + `tasks/M*_tasks.yaml` | Граф Task: `id`, `depends_on` |
| `agent/effort-estimates.yaml` (Этап 3) | Трудоёмкость каждой Task — не оценивается заново этим правилом |

**Контракт с `effort-estimates.yaml`:** правило ожидает по каждой Task
запись вида `<task_id>: <effort_days>` (трудоёмкость в человеко-днях).
Если структура файла Этапа 3 отличается — сначала приводится к этому
виду (единица измерения и ключ `task_id` должны совпадать с `id` в
`tasks/M*_tasks.yaml`), правило распределения не меняется.

> На момент написания этого правила файла `agent/effort-estimates.yaml`
> в репозитории нет (см. `open-issues.md`). Правило фиксирует контракт
> заранее; без него сам расчёт объёма (шаг 3) невозможно выполнить, но
> это ограничение среды, а не порок правила.

## Константы v1 (не входят в `input-schema.json`)

Поля под них в `input-schema.json` сейчас нет. Добавлять новое поле в
`input-schema.json` без явного согласования нельзя (см. `CLAUDE.md`), 
поэтому обе константы зафиксированы здесь и помечены как ограничение v1:

- **`sprint_length_weeks = 2`** — длительность спринта. Кандидат в
  input-schema v2 (например, `methodology.sprint_length_weeks`), если
  потребуется конфигурировать per-проект.
- **`capacity_per_sprint_days = sprint_length_weeks * 5`** — доступная
  трудоёмкость на спринт, из допущения "1 консультант, полная
  занятость, 5 рабочих дней в неделю". Кандидат в input-schema v2
  (например, `team_size` / `consultants_count`), если потребуется
  учитывать команду из нескольких консультантов или неполную занятость.

Обе константы — точечное ограничение v1, а не архитектурное решение
шаблона; расширение (новые поля схемы входа) выносится как предложение
для v2, а не вводится этой правкой.

## Алгоритм распределения

1. **Построить единый граф зависимостей** по всем Task всех Milestone
   (M1–M9), используя `depends_on` — включая межмилестоновые связи
   (например, `T-6.4.1 depends_on T-1.3.2`, `T-5.2.1 depends_on T-2.3.1`).
   `used_by` не используется как источник порядка (см. `open-issues.md`,
   п.1 — часть `used_by` указывает на Milestone/WBS, а не на Task).

2. **Определить порядок Task** топологической сортировкой графа
   зависимостей. Если несколько Task не связаны зависимостями между
   собой (нет ни явного `depends_on`, ни цепочки через него) — порядок
   тай-брейка берётся из порядка их следования в шаблоне (Milestone →
   WBS → Task, как в `tasks/M*_tasks.yaml`), т.к. эта
   последовательность уже отражает содержательный порядок работ.

3. **Жадно распределить Task по спринтам** в этом порядке:
   - Task может быть назначена в спринт `N` только если все её
     `depends_on`-предшественники назначены в спринт `≤ N` (предполагается,
     что внутри одного спринта Task выполняются последовательно в том же
     порядке, что и в шаблоне).
   - Task добавляется в спринт `N`, если в нём остаётся свободная
     трудоёмкость (`capacity_per_sprint_days` минус уже назначенное);
     иначе — переходит в спринт `N+1`.
   - Трудоёмкость Task берётся из `effort-estimates.yaml` (см. контракт
     выше); Task без записи в `effort-estimates.yaml` не может быть
     распределена — это ошибка входных данных, а не повод пропустить
     Task молча.

4. **Посчитать доступные спринты проекта:**
   `available_sprints = ceil((target_launch_date - start_date) / (sprint_length_weeks * 7 дней))`.

5. **Сравнить и зафиксировать результат:**
   - `total_sprints_used ≤ available_sprints` → план распределения
     фиксируется как есть (номера спринтов — результат генерации для
     конкретного проекта, в шаблон не переносятся).
   - `total_sprints_used > available_sprints` → генерация **не
     блокируется**; агент формирует явное предупреждение (risk/flag) с
     фактическими числами (`total_sprints_used`, `available_sprints`,
     дефицит в спринтах/неделях), чтобы консультант/PM принял решение
     (сдвинуть `target_launch_date`, увеличить
     `capacity_per_sprint_days`, пересмотреть объём). Решение — за
     человеком, не за агентом.

## Границы

- Правило не меняет `schema/milestones_wbs.yaml` и `tasks/M*_tasks.yaml`.
- Номера спринтов не фиксируются в шаблоне — вычисляются агентом
  индивидуально для каждого проекта на основе его `input-schema.json`.
- Любое расширение (новые поля `input-schema.json` под длительность
  спринта/capacity команды, учёт параллельных консультантов, учёт
  нерабочих дней/праздников) не вводится по ходу — выносится как
  предложение для v2.
